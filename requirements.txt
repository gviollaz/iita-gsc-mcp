# Upper bound is load-bearing: mcp 2.0.0 removed mcp.server.fastmcp, which
# main.py imports. Without it a rebuild resolves to 2.x and the server dies
# on startup.
mcp[cli]>=1.9.0,<2.0.0
pydantic>=2.0
google-api-python-client>=2.90.0
google-auth>=2.20.0
uvicorn>=0.27.0
starlette
PyJWT>=2.8.0
